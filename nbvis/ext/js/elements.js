// file nbvis/js/elements.js
/* SVG/Canvas configuration for the nbvis module
   Assembled by Eric Easthope for Callysto
   MIT License
*/

// append a container to the cell output and set its dimensions
d3.select(element[0]).append(#element_type)
    .attr("id", #name)
    .attr("width", #width)
    .attr("height", #height);